# Public Repository Notes

This repository is public, but no root license is currently declared. Reuse rights are not granted unless a license is added later.

Runtime data, generated outputs, local assistant tooling, browser profiles, queues, raw API outputs, and private environment files should remain untracked.
